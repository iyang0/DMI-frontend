import styled from 'styled-components';

const Svg = styled.svg`
  background-color: rgb(35, 0, 73);
  position: relative;
  width: 100%;
  margin: 0 auto;
  display: block;
  z-index: 1100;
`;

export default Svg;
